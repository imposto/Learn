// vetor de variaveis, preenchidas com valores
const [a, b, c, d, e] = [5, 8, 9, 4, 2]

const soma = a + b + c + d + e
const subtracao = soma - soma
const multplicacao = a * b * c * d * e
const modulo = a % 2
console.log(soma, subtracao, -multplicacao, modulo)
